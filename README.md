# Installation & settings

## Conda environment & packages

    $ conda create -n env
    $ conda activate env

TODO: install requirements

    (env) $ 
    (env) $ cd crotalus
    (env) $ pip install -e .

# Web interface



